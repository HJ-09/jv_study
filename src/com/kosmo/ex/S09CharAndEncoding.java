package com.kosmo.ex;

public class S09CharAndEncoding {
    public static void main(String[] args) {
        char c='a'; //''⇒ 문자타입 utf-16 (2byte 크기)
        System.out.println((int)c); //→ 97
        c='한';
        System.out.println((int)c); //→54620
        System.out.println(Integer.toHexString(54620));  //→d55c
        c='\ud55c';
    }
    /*자바 `char` 타입과 유니코드 관련 내용을 포함한 `L10Char` 클래스 코드에 대해, 핵심 개념과 추가 정보까지 함께 정리해드릴게요.

---

# 자바 char 타입과 문자 인코딩 정리 (L10Char 수업 내용)

---

## 1. `char` 타입 개요

* 자바 `char`는 **16비트(2바이트)** 크기를 가진 기본형(숫자니까~) 타입
* 내부적으로는 유니코드(Unicode) 코드 포인트 중 0~65535(2byte 크기) 범위(즉, U+0000 ~ U+FFFF)를 저장
* 즉, **UTF-16 인코딩 단위 한 개**와 대응
* 따라서 2바이트를 초과하는 문자는 `char` 하나로 표현 불가능 (예: 일부 이모지)

---

## 2. 문자 인코딩 방식 비교

| 인코딩   | 특징                           | 자바 내 사용            |
| ------- | ---------------------         | ---------------       |
| ASCII   | 7비트(0~127) 문자 표준         | 자바는 ASCII 호환       |
| MS949   | 윈도우 한글 문자 인코딩          | Windows 전용           |
| Unicode | 국제 표준 문자 집합 (16비트 기본) | 자바 `char` 기반        |
| UTF-8   | 가변 길이 1~4 바이트            | 일반 문서, 네트워크 데이터 |
| UTF-16  | 가변 길이 2 or 4 바이트         | 자바 내부 문자열 표현     |

---

## 3. `char`와 유니코드 코드 포인트

```java
char c = 'A';          // 'A' 문자 → ASCII 65
System.out.println((int)c); // 출력: 65

char c2 = '가';        // '가' 문자 → 유니코드 44032 (0xAC00)
System.out.println((int)c2); // 출력: 44032
System.out.println(Integer.toHexString((int)c2)); // 출력: ac00 (← 크기, 위치 보려고 출력)
```

* `(int)`로 `char`를 정수형으로 변환하면 코드 포인트 값 출력
* 16진수 출력 시 `Integer.toHexString()` 사용

---

## 4. 2바이트 초과 문자(서로게이트 페어)

* 예: `😍` 이모지 (코드포인트 U+1F60D, 128525)는 16비트 넘음
* 자바 `char`는 한 문자를 표현할 수 없고 **2개의 char 배열**(서로게이트 페어)로 저장
* 이모지 표현 예:

```java
String s = "\uD83D\uDE0D";  // 서로게이트 페어 2개 char로 이모지 저장
System.out.println(s);       // 😍 출력
```

---

## 5. 유니코드 이스케이프(탈출문자) → 역 슬러시 \ 사용.
\n : 엔터 라인개행
\t : 탭
⇒ 문자대신 키보드 키...? 뭐라고..?

* \ uXXXX 는 16진수 4자리 유니코드 코드포인트 표현
* 예:

```java
char u = '\uAAAA';
System.out.println(u); // ꪪ 출력
```

---

## 6. 정수 값을 이용한 문자 표현

* `char`는 내부적으로 숫자이기 때문에 정수 할당 가능
* 예:

```java
char n = 80;
System.out.println(n); // 'P' 출력

n = 60000;
System.out.println(n); // 60000에 해당하는 유니코드 문자 출력 (제어문자 등)
```

---

## 7. 추가 지식: `char`와 `String` 차이

| 구분               | `char`             | `String`                            |
| ------------      | ----------------   | --------------------------          |
| 크기               | 2바이트             | 여러 `char`의 집합                    |
| 표현 가능한 문자 범위 | U+0000 ~ U+FFFF    | UTF-16 문자열, 서로게이트 페어 포함 가능 |
| 사용 예            | 단일 문자            | 여러 문자, 단어, 문장                  |

---

## 8. 유니코드 문자 처리 팁

* `String.codePointAt(index)`로 정확한 유니코드 코드 포인트 얻기
* `Character.toChars(int codePoint)`로 코드 포인트 → `char[]` 변환 가능
* 이모지 등 4바이트 문자를 다룰 때 유용

---

## 9. 요약

* 자바 `char`는 2바이트 고정 크기
* 기본 문자(영문, 한글 등)는 한 개 `char`로 표현 가능
* 이모지 등 4바이트 문자는 2개 `char`로 표현 (서로게이트 페어)
* 유니코드는 전세계 문자를 통합한 표준 문자 집합
* UTF-8은 가변 길이 인코딩, UTF-16은 자바 문자열 내부 인코딩 방식
---


---

# 자바 char & 유니코드 문제 5개

---

### 1. `char` 변수에 문자 `'Z'`를 저장하고 정수값으로 출력하는 코드를 작성하세요.

>> 정답 : 90
char n='Z';
System.out.println((int)n);

### 2. 문자 `'한'`의 유니코드 정수값을 출력하는 코드를 작성하세요.

>> 정답 : 54620
char m='한';
System.out.println((int)m);

### 3. 다음 코드의 출력 결과를 예상하세요.

```java
char c = 50000;
System.out.println(c);
System.out.println((int)c);
```
---
>> 정답 : 썐 / 50000

### 4. 유니코드 이스케이프 `\u0041`은 어떤 문자를 나타내나요? 해당 문자를 출력하는 코드를 작성하세요.
>> 정답 : A
String s="\u0041";
        System.out.println(s);
---

### 5. 다음 중 자바 `char` 타입 변수에 저장할 수 없는 것은 무엇인가요?

A. `'A'`
B. `'\uAC00'`
C. `'😍'`
D. `65`

>> 정답 : C


# 자바 `char`와 유니코드 면접 대비 질문 9선

---

### 1. 자바에서 `char` 타입은 무엇인가요? 크기와 특징을 설명해 주세요.
>> 2byte, 기본형 타입
### 2. 자바 `char` 타입으로 모든 유니코드 문자를 표현할 수 있나요? 왜냐하면?
>> 불가능. 유니코드는 utf-8, utf-16 두 가지 전부 2byte 넘음
### 3. 유니코드와 UTF-8, UTF-16의 차이를 설명해 주세요.
>> utf-8은 일반적인 문서, utf-16은 java
### 4. 자바에서 4바이트 문자를 다루려면 어떻게 해야 하나요?
>> 2개의 char 배열로 사용(서로게이트 페어)
### 5. `char`와 `String`의 차이점은 무엇인가요?
>> char는 단일문자, String은 배열(문자열)
### 6. 유니코드 이스케이프 시퀀스( / uAB12 )란 무엇인가요? 예를 들어 보세요.
>>
### 7. `char` 변수를 정수형으로 변환하면 무슨 값이 나오나요?
>>
### 8. 자바에서 `char` 타입에 음수 값을 저장할 수 있나요?
>> 아니욤. 효과적으로 저장하기 위해서 부호비트 없이 양수만 저장함
### 9. 자바 문자열이 내부적으로 어떤 인코딩을 사용하는지 아나요?
>>

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>정답!<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<<

### 1. 자바에서 `char` 타입은 무엇인가요? 크기와 특징을 설명해 주세요.

* **답변:** 2바이트 크기의 기본형 타입으로, UTF-16 인코딩 기준의 유니코드 문자를 저장합니다.

---

### 2. 자바 `char` 타입으로 모든 유니코드 문자를 표현할 수 있나요? 왜냐하면?

* **답변:** 아니요, `char`는 2바이트로 표현 가능한 BMP(기본 다국어 평면) 문자는 저장 가능하지만, 4바이트가 필요한 이모지 같은 문자는 두 개의 `char`(서로게이트 페어)로 표현합니다.

---

### 3. 유니코드와 UTF-8, UTF-16의 차이를 설명해 주세요.

* **답변:**

  * 유니코드는 전 세계 문자를 표현하기 위한 문자 집합 표준입니다.
  * UTF-8은 가변 길이(1\~4바이트) 인코딩 방식이며, 웹과 문서에서 널리 쓰입니다.
  * UTF-16은 2바이트 또는 4바이트 단위 가변 길이 인코딩으로, 자바 내부 문자열 표현에 사용됩니다.

---

### 4. 자바에서 4바이트 문자를 다루려면 어떻게 해야 하나요?

* **답변:** `String`에서 서로게이트 페어(`char` 배열 2개)를 사용하거나, `int` 타입으로 코드포인트를 저장하고 `Character.toChars()` 메서드를 사용해 처리합니다.

---

### 5. `char`와 `String`의 차이점은 무엇인가요?

* **답변:** `char`는 단일 16비트 문자 저장용 기본형 타입이고, `String`은 여러 `char`의 집합인 불변 참조형 객체입니다.

---

### 6. 유니코드 이스케이프 시퀀스란 무엇인가요? 예를 들어 보세요.

* **답변:** `\ u` 뒤에 4자리 16진수 코드를 붙여 특정 유니코드 문자를 표현하는 방식입니다. 예: `'\uAC00'`는 한글 '가'입니다.

---

### 7. `char` 변수를 정수형으로 변환하면 무슨 값이 나오나요?

* **답변:** 문자에 대응하는 유니코드 코드포인트 정수가 나옵니다.

---

### 8. 자바에서 `char` 타입에 음수 값을 저장할 수 있나요?

* **답변:** 아니요. `char`는 부호 없는 16비트 정수형으로 0~65535 범위만 저장 가능합니다.

---


### 9. 자바 문자열이 내부적으로 어떤 인코딩을 사용하는지 아나요?

* **답변:** UTF-16 인코딩을 사용하며, 문자 하나는 2바이트 `char` 또는 서로게이트 페어(4바이트)로 표현됩니다.

---


*/
}
